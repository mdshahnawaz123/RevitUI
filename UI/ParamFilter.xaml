<Window x:Class="RevitUI.UI.ParamFilter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parameter Filter"
        Height="300" Width="505"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 🔹 CATEGORY -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Category:" Width="100" VerticalAlignment="Center"/>
            <ComboBox x:Name="CategoryCombo"
                      SelectionChanged="ElementWhenCategoryChanged"
                      Width="250"
                      DisplayMemberPath="Name"/>
        </StackPanel>

        <!-- 🔹 RULE BUILDER -->
        <Border Grid.Row="1" Padding="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                <!-- Parameter -->
                <ComboBox x:Name="ParameterCombo"
                          Width="150"
                          Margin="0,0,10,0"/>

                <!-- Operator -->
                <ComboBox x:Name="RuleCombo" Width="150" Margin="5,0,10,0">
                    <ComboBoxItem Content="Equals"/>
                    <ComboBoxItem Content="Contains"/>
                    <ComboBoxItem Content="Begins With"/>
                    <ComboBoxItem Content="Ends With"/>
                    <ComboBoxItem Content="Greater Than"/>
                    <ComboBoxItem Content="Less Than"/>
                </ComboBox>

                <!-- Value -->
                <TextBox x:Name="ValueBox"
                         VerticalContentAlignment="Center"
                         Width="120"
                         Margin="0,0,10,0"/>
            </StackPanel>
        </Border>

        <!-- 🔹 ACTION BUTTONS -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Apply Filter"
                    Width="120"
                    Margin="5"
                    Click="OnApplyFilter"/>

            <Button Content="Isolate"
                    Width="100"
                    Margin="5"
                    Click="OnIsolate"/>

            <Button Content="Clear"
                    Width="100"
                    Margin="5"
                    Click="OnClear"/>

            <Button Content="Close"
                    Width="100"
                    Margin="5"
                    Click="OnClose"/>
        </StackPanel>

    </Grid>
</Window>